# Preset: everything mocked — no Ollama, no endpoint, no model downloads.
#   APP_ENV_FILE=.env.mock docker compose up app --build
# Loaded ON TOP of .env (overrides it), so a configured .env is fine to keep.
VISION_BACKEND=mock
ROUTE_A_PROVIDER=mock
